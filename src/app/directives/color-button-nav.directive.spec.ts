import { ColorButtonNavDirective } from './color-button-nav.directive';

describe('ColorButtonNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorButtonNavDirective();
    expect(directive).toBeTruthy();
  });
});
